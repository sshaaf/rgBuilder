[package]
name = "rbuilder-pipeline"
version = "0.4.2"
edition.workspace = true
rust-version.workspace = true
description = "Parallel processing pipeline for rBuilder"
license = "MIT OR Apache-2.0"

[dependencies]
rbuilder-error = { path = "../rbuilder-error" }
rbuilder-extraction = { path = "../rbuilder-extraction" }
rbuilder-graph = { path = "../rbuilder-graph" }
rbuilder-registry = { path = "../rbuilder-registry" }
indicatif = "0.17"
rayon = "1.7"
crossbeam = "0.8"

[dev-dependencies]
tempfile = { workspace = true }
rbuilder-languages = { workspace = true }
