[package]
name = "rgbuilder-error"
version = "0.4.3"
edition.workspace = true
rust-version.workspace = true
description = "Error types for rgBuilder"
license = "MIT OR Apache-2.0"

[dependencies]
rgbuilder-plugin-api = { path = "../rgbuilder-plugin-api" }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "1"
toml = "0.8"

[features]
default = []
